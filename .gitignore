# ===============================================
# .gitignore CONSOLIDADO PARA PORTAFOLIO PYTHON
# ===============================================

# 1. ENTORNO VIRTUAL
venv/
.venv/
env/
*~

# 2. ARCHIVOS TEMPORALES Y DE COMPILACIÓN DE PYTHON
__pycache__/
*.pyc
*.o
*.pyd
*.so

# 3. BASES DE DATOS Y PERSISTENCIA GENERADA (OUTPUT)
# Bases de datos SQLite (P6, P9, P13, P15)
*.sqlite3
*.db

# Archivos de persistencia JSON de SALIDA (OUTPUT)
# Ignora el JSON de la librería (P2)
Proyecto02_Gestor_Libreria/libreria.json 

# Archivo de salida consolidado (P10)
reporte_consolidado.csv

# 4. ARCHIVOS GENERADOS POR FRAMEWORKS WEB (Django, Flask)
/media/
/static/

# Django: Mantener el historial de migraciones. Ignoramos la carpeta de caché de Flask.
instance/

# 5. ARCHIVOS DE CONFIGURACIÓN Y SECRETOS (Claves API)
# ¡Importante para proyectos con APIs! (P7, P14)
config.py
*.env
